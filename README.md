# zuriflightbookingapi
Zuri flight booking api, to test the api, run npm install, then create a .env file and pass in your mongodb connection string
Zuri flight booking api, to test the api, run npm install, then create a .env file and pass in your mongodb connection string
